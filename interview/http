httpSever.Serve(listener) 和 http.ListenAndServe()
// 阻塞在这里 注册handler
// request 从该入口进入 先block阻塞 会把整个子协程完全block住
// listen -> mux regiter route -> real handler
// 每个handler用不同的goroutine处理
// goroutine轻量级协程 每个goroutine占4K大小 可以瞬间创建 几千个
// golang做http server有得天独厚的优势 把多核心特性利用起来

var(
	// X- 自定义header field
	HEADER_FIELD_SESSION_ID = "X-Session-Id"
	HEADER_FIELD_USER_NAME  = "X-User-Name"
)


